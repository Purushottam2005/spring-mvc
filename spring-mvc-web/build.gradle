apply plugin: 'war'

war{ archiveName 'spring-mvc-web' }

dependencies{
	compile project (':spring-mvc-data')

	compile 'org.springframework:spring-webmvc:3.2.4.RELEASE'

	compile 'org.thymeleaf:thymeleaf-spring3:2.0.18'

	testCompile 'junit:junit:4.+'
}